#ifndef IFRAMEBUFFER_TS
#define IFRAMEBUFFER_TS

module akra {
	export enum EFrameBufferTypes {
		COLOR  = 0x1,
        DEPTH   = 0x2,
        STENCIL = 0x4
	}

	export interface IFrameBuffer {

	}
}

#endif

