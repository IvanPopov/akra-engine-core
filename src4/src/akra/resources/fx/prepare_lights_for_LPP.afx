include "apply_lights_and_shadows.afx";

float4 prepare_light_omni() : COLOR {
	for(int i = 0; i < nOmni; i++){
		if(isPrepareAll){
			result_color += applyBaseLightParams(pixelData, points_omni[i].lightData);
		}
	}
	
	return float4(result_color.xyz, 1.);
}